name: ${artifactId}
version: "1.0-SNAPSHOT"
author: Your Name
main: ${package}.${pluginFile}
commands:
  exampleCommand:
    description: Execute example command
    usage: /<command>
    permission: <plugin name>.basic
    permission-message: You don't have <permission>
