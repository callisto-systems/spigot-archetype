name: ${artifactId}
version: ${version}
author: ${developerName}
main: ${package}.${pluginClassName}
commands:
  CoderDojo:
    description: Execute example command
    usage: /<command>
    permission: <plugin name>.basic
    permission-message: You don't have <permission>
